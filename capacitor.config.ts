import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tercer_Parcial_IPDM',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
